@model RpshopingMvc.Models.tb_Favorites

@{
    ViewBag.Title = "删除选品库";
    Layout = "~/Views/Shared/_LayoutBackground.cshtml";
}
<ol class="breadcrumb">
    <li><a href="@Url.Action("Index")">商品分类</a></li>
    <li>删除商品分类</li>
</ol>
<div class="roleGroupCreateEdit">
    <h3>请确认以下信息是否要删除</h3>
    <div>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Name)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Explain)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Explain)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Type)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Type)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="删除" class="btn btn-default" />
                <a href="@Url.Action("Index")" class="btn btn-default" style="margin-left:60px;">返回</a>
            </div>
        }
    </div>
</div>